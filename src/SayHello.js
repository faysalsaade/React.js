function SayHello({ name, age, Nationality }) {
  return (
    <div>
      <h1> hello {name}</h1>
      <h2> my age is {age}</h2>
      <h3>I am {Nationality}</h3>
    </div>
  );
}
export default SayHello;
